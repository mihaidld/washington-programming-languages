; Programming Languages, Dan Grossman
; Section 5: Racket Introduction

; always make this the first (non-comment, non-blank) line of your file
#lang racket ;tells DrRacket there is Racket code in this file since DrRacket supports multiple languages, will interpret it as Racket code

; not needed here, but a workaround so we could write tests in a second file, make everything in this file public
; see getting-started-with-Racket instructions for more explanation
; each file is a module, by default everything in it is private, need to specify what to make available to other files

(provide (all-defined-out))

; basic definitions
(define s "hello")
